/* 루트 서브패스는 **바닥값**이다 — 소비처가 컴포넌트를 하나도 쓰지 않을 때 무는
 * 것만 여기 산다(ADR-0017). 컴포넌트는 `@flameware/ui/button`처럼 저마다의
 * 서브패스로 들어온다(ADR-0023 §4). 여기서 재수출하면 트리 셰이킹이 되든 안 되든
 * 바닥값을 재는 저울이 컴포넌트 무게를 같이 재게 된다. */
export { cn } from "./lib/utils.js"
