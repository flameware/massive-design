import { Canvas, Controls, Meta, Story, Title } from "@storybook/addon-docs/blocks"

import { StatusBadge } from "../StatusBadge"
import * as ToggleStories from "./Toggle.stories"

<Meta of={ToggleStories} />

<Title />

<StatusBadge status={ToggleStories.default.parameters.ds.status} /> · since {ToggleStories.default.parameters.ds.since}

누른 상태가 남는 버튼이에요. 즐겨찾기, 굵게나 기울임 같은 서식 버튼, 사이드바
펼치기에 써요.

## 언제 쓰는가

- 켜고 끄기 하나. 즐겨찾기, 서식 버튼, 사이드바 펼치기가 여기에 해당해요.
- 혼자 쓰이는 토글 하나. 여럿을 한 판으로 묶고 화살표로 옮겨 다니게 하려면
  `ToggleGroup`을 쓰세요.

## 언제 쓰지 않는가

- 누르면 바로 무언가 일어나고 상태가 남지 않는다면 `Button`이에요.
- 여럿 중 하나 또는 여럿을 화살표로 옮겨 고른다면 `ToggleGroup`이에요.

## 크기

`size` 옵션은 `sm`, `md`, `lg` 세 가지예요. 값 이름은 `Button`의 `size`와
같고, 기본은 `md`예요.

<Canvas of={ToggleStories.Sizes} />

`sm`은 보이는 높이가 24px보다 작지만, 누를 수 있는 영역은 24×24까지 자동으로
넓어지니 크기를 따로 키우지 않아도 돼요. 아래 스토리가 그 크기를 실제로 재는
대상이에요.

<Canvas of={ToggleStories.Size24} />

## 상태

<Canvas of={ToggleStories.States} />

## 제어와 비제어

`pressed`와 `onPressedChange`를 주면 값을 앱이 관리해요. `defaultPressed`만
주면 컴포넌트가 스스로 관리해요.

<Canvas of={ToggleStories.ControlledVsUncontrolled} />

## Form 안에서

`name`을 주면 지금 눌린 값이 제출할 때 함께 담겨요. 따로 연결할 것은 없어요.

<Canvas of={ToggleStories.InForm} />

## 키보드

Tab으로 닿고 Enter나 Space로 눌러요. 누를 때마다 눌린 상태가 뒤집혀요.

<Canvas of={ToggleStories.Keyboard} />

## Props

<Story of={ToggleStories.Playground} />

<Controls of={ToggleStories.Playground} />

## 배경

Base UI Toggle 위에 색과 치수만 입혔어요. Base UI Toggle은 Checkbox나 Select와
달리 스스로 폼에 값을 내지 않아서, `name`을 받으면 지금 눌린 값을 그대로
따라가는 숨은 입력 하나를 곁에 세워요.

`size`는 이 컴포넌트의 옵션이에요. 화면 전체의 밀도를 한 번에 낮추는 기능은
제공하지 않아요([ADR-0023](https://github.com/flameware/massive-design/blob/main/docs/adr/0023-second-generation-base-ui.md)
§11).
