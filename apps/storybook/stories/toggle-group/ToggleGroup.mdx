import { Canvas, Controls, Meta, Story, Title } from "@storybook/addon-docs/blocks"

import { StatusBadge } from "../StatusBadge"
import * as ToggleGroupStories from "./ToggleGroup.stories"

<Meta of={ToggleGroupStories} />

<Title />

<StatusBadge status={ToggleGroupStories.default.parameters.ds.status} /> · since {ToggleGroupStories.default.parameters.ds.since}

`Toggle` 여럿을 한 판으로 묶어 화살표로 옮겨 다니게 해요. 기간 필터나 보기
전환처럼 선택지를 가로로 늘어놓을 때 써요.

## 언제 쓰는가

- 서로 배타적인 선택지를 가로로 늘어놓을 때. 기간 필터(1주, 1개월, 1년),
  보기 전환(목록, 격자)이 여기에 해당해요.
- 여러 개를 동시에 켜야 할 때. `multiple`을 주면 서식 버튼 모음(굵게,
  기울임, 밑줄)처럼 쓸 수 있어요.

## 언제 쓰지 않는가

- 선택지가 많아 가로로 들어가지 않으면 `Select`를 쓰세요.
- 묶을 것이 없는 토글 하나라면 `Toggle` 혼자로 충분해요.

## 사용법

항목은 그냥 `Toggle`이에요. 그룹 전용 항목 컴포넌트를 따로 쓰지 않아요.

<Canvas of={ToggleGroupStories.Playground} />

<Canvas of={ToggleGroupStories.Multiple} />

## 제어와 비제어

`value`와 `onValueChange`를 주면 값을 앱이 관리해요. `defaultValue`만 주면
컴포넌트가 스스로 관리해요. 값은 선택된 항목들의 배열이에요. `multiple`이
기본으로 꺼져 있어서 평소에는 원소가 0개나 1개예요.

<Canvas of={ToggleGroupStories.ControlledVsUncontrolled} />

## Form 안에서

`name`을 주면 선택된 값이 제출할 때 함께 담겨요. `multiple`일 때는 값을
쉼표로 이어서 내요.

<Canvas of={ToggleGroupStories.InForm} />

## 키보드

Tab은 그룹에 한 번만 닿고, 그다음부터는 화살표로 항목 사이를 옮겨요. Tab이
처음 닿는 곳은 눌린 항목이 아니라 첫 항목이에요. Enter나 Space가 포커스가
놓인 항목을 눌러요.

<Canvas of={ToggleGroupStories.Keyboard} />

## Props

<Story of={ToggleGroupStories.Playground} />

<Controls of={ToggleGroupStories.Playground} />

## 배경

Base UI ToggleGroup 위에 색과 치수만 입혔어요. 항목을 그룹 전용 컴포넌트로
나누지 않은 이유는 Base UI의 구조가 그렇게 평평하기 때문이에요.

Base UI ToggleGroup은 Toggle과 마찬가지로 스스로 폼에 값을 내지 않아요.
그래서 `name`을 받으면 선택된 값을 숨은 입력 하나로 내요. `multiple`일 때
쉼표로 이어 내는 방식은 지금까지 확인된 쓰임이 단일 선택이라 이대로 두고
있어요.
