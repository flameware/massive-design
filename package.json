{
  "name": "massive-design",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "node --test scripts/*.test.mjs && bun run --filter '*' test",
    "sync:preflight": "node scripts/sync-preflight.mjs",
    "sync:checklist": "cd packages/ui && bun ../../scripts/sync-checklist.mjs",
    "sync:review-storybook": "node scripts/review-storybook.mjs",
    "pointer-gate": "node scripts/pointer-target-gate.mjs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  }
}
