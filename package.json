{
  "name": "massive-design",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "node --test scripts/*.test.mjs && bun run --filter '*' test",
    "sync:preflight": "node scripts/sync-preflight.mjs",
    "sync:review-storybook": "node scripts/review-storybook.mjs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  }
}
